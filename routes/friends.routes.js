const express = require('express');
const router = express.Router();

// #Neo4J

router.post('/', (req, res, next) => {
    // Get user1, user2
    // If already exist, return 200
});

router.delete('/', (req, res, next) => {
   // Get user1, user2
   // If not exist, return 200
});
